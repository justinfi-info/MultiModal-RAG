from setuptools import find_packages, setup

setup(
    name='multimodal_rag',
    version='0.1.0',
    author='justinfi.info',
    author_email='justinfi.info@gmail.com',
    description='Multimodal RAG and its real-world applications to process text, images, PDFs, and other data formats',
    packages=find_packages(),
    install_requires=[],
)